# queries
type Query {
  company(id: ID!): Company
  jobs: [Job]
  job(id: ID!): Job
}

# types
type Job {
  id: ID!
  title: String
  description: String
  company: Company
}

type Company {
  id: ID!
  name: String
  description: String
  jobs: [Job]
}
